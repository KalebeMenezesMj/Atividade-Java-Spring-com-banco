spring.application.name=JPASpring

# Configuração da fonte de dados (DataSource)
spring.datasource.url=jdbc:postgresql://localhost/db_fatec
spring.datasource.username=postgres
spring.datasource.password=kalebemmj

# Configuração do Driver do Postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuração do Hibernate
spring.jpa.hibernate.ddl-auto=update
hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect